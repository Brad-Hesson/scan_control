[package]
name = "image-compute-shader-builder"
version = "0.1.0"
edition = "2024"
publish = false

# This is intentionally a separate workspace so rust-gpu's nightly-only build
# dependencies never become part of the stable host workspace.
[workspace]

[dependencies]
spirv-builder = "=0.10.0-alpha.1"
